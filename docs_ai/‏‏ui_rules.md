

## Design Tokens
- **Primary Color**: Dark Blue (#00008b).
- **Secondary Color**: Slate Gray (#708090).

## Accessibility & RTL
- **Rule**: Every interactive element must have an `aria-label`.
- **RTL Support**: All Hebrew layouts must use CSS logical properties (e.g., `margin-inline-start`).